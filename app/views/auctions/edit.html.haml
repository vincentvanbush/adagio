%h1 Editing auction

= simple_form_for [category, auction] do |f|
  = render 'shared/error_messages', object: f.object
  .inputs
    = f.input :category_id, collection: Category.all
    = f.input :description, as: :text
  .actions
    = f.button :submit
